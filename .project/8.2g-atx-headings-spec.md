# Phase 8.2g — ATX headings（`#` 标题，CommonMark 4.2）

## 目标
修复 ATX headings 节 6 条 STRUCT 失败（现 12/18 → 目标 18/18）。根因全部自包含在 `Blocks.ts` 的 `parseHeading` + 入口检测，**零碰列表/链接/内联/Setext/Thematic break**。预计 +6。

## 现状（要替换的代码，`entry/src/main/ets/parser/commonmark/Blocks.ts`）

### 入口检测（第 45 行）
```ts
if (line.startsWith('#')) {
  return parseHeading(state);
}
```
缺陷：① 不容忍 0-3 前导空格（` ### foo` 进不来 → 落段落，Ex68/71-line2）；② 不校验 `#` run 长度 1-6（`#######` 7 个也进，Ex63）；③ 不校验 `#` run 后必须跟 空格/Tab/行尾（`#5`/`#hashtag` 误当标题，Ex64）。

### `parseHeading`（第 171-192 行）
```ts
let level = 0;
for (let i = 0; i < line.length && line[i] === '#'; i++) { level++; }  // 从 line[0] 数，不跳前导空格
node.attrs.level = level > 6 ? 6 : level;                              // 7+ clamp 到 6（错，应非标题）
let text = line.substring(level).trim();
let endIdx = text.length - 1;
while (endIdx >= 0 && text[endIdx] === '#') { endIdx--; }              // 无脑剥所有尾随 #（不查前面是否空格）
text = text.substring(0, endIdx + 1).trim();
parseInlines(text, node);
```
缺陷：尾随 `#` 剥离不查「前面是否空格」→ `# foo#` 剥成 `foo`（Ex75，应留 `foo#`）；`### foo \###` 剥成 `foo \`（Ex76，应留 `foo \###` → inline 解 `\#` → `foo ###`）。

## CommonMark 规则（本批落地）
1. **开标记**：≤3 前导空格 + **1-6 个** `#`，其后**必须**跟 空格/Tab 或**行尾**（仅 `#` 后接非空白如 `#5` → 不是标题）。`#######`(7) → 不是标题。
2. **内容**：开标记之后到行尾，首尾去 空格/Tab。
3. **闭合序列**：内容尾部的一段 `#` run，**仅当其前是 空格/Tab（或该 run 即整个内容）** 才视为闭合序列并整段剥除（连同其前的空格/Tab）；否则 `#` 属内容保留。闭合 run 后只能跟空格/Tab。
4. `level>6` / `#` 后非空白 / 前导空格≥4 → 整行不是 ATX 标题，回退普通段落。

## 实现（只改 Blocks.ts，无正则，具名 class）

### 新增（放 parseHeading 上方）
```ts
/** ATX 开标记解析结果（具名 class，禁匿名对象字面量） */
class AtxOpen {
  level: number = 0;
  contentStart: number = 0;
}

/** 解析 ATX 开标记；不是合法 ATX 标题返回 null。
 *  规则：≤3 前导空格 + 1-6 个 # + 其后跟 空格/Tab/行尾。 */
function parseAtxOpen(line: string): AtxOpen | null {
  let i: number = 0;
  while (i < line.length && line[i] === ' ' && i < 4) i++;
  if (i >= 4) return null;                       // ≥4 空格 → 缩进代码，非标题
  const start: number = i;
  while (i < line.length && line[i] === '#') i++;
  const level: number = i - start;
  if (level < 1 || level > 6) return null;        // 0 个或 7+ 个 → 非标题
  if (i < line.length) {                          // run 后必须空格/Tab（行尾 i===length 也合法）
    const c: string = line[i];
    if (c !== ' ' && c !== '\t') return null;
  }
  const ah: AtxOpen = new AtxOpen();
  ah.level = level;
  ah.contentStart = i;
  return ah;
}

/** 入口用：是否合法 ATX 标题开始 */
function isAtxHeadingStart(line: string): boolean {
  return parseAtxOpen(line) !== null;
}
```

### 改入口（第 45 行）
```ts
if (isAtxHeadingStart(line)) {
  return parseHeading(state);
}
```

### 重写 parseHeading（第 171-192 行）
```ts
export function parseHeading(state: ParseState): AstNode {
  const node: AstNode = new AstNode(AstNodeType.Heading);
  const line: string = state.currentLine();
  state.nextLine();
  const ah: AtxOpen | null = parseAtxOpen(line);
  // 入口已保证非 null；防御性处理
  const level: number = ah !== null ? ah.level : 1;
  const cStart: number = ah !== null ? ah.contentStart : 1;
  node.attrs.level = level;

  // 内容：开标记之后，首尾去空格/Tab
  let s: string = line.substring(cStart);
  // 去首部空格/Tab
  let a: number = 0;
  while (a < s.length && (s[a] === ' ' || s[a] === '\t')) a++;
  // 去尾部空格/Tab
  let e: number = s.length;
  while (e > a && (s[e - 1] === ' ' || s[e - 1] === '\t')) e--;
  s = s.substring(a, e);

  // 闭合序列：尾部 # run，仅当其前是 空格/Tab 或 run 即整个内容 才剥除
  let he: number = s.length;                         // run 末端（已无尾随空白）
  let h: number = he;
  while (h > 0 && s[h - 1] === '#') h--;             // [h, he) 为尾部 # run
  if (h < he && (h === 0 || s[h - 1] === ' ' || s[h - 1] === '\t')) {
    // 是闭合序列 → 剥除 run + 其前的空格/Tab
    let k: number = h;
    while (k > 0 && (s[k - 1] === ' ' || s[k - 1] === '\t')) k--;
    s = s.substring(0, k);
  }

  parseInlines(s, node);
  return node;
}
```

## 逐例核对（手验）
- Ex63 `#######  foo` → run=7 → parseAtxOpen null → 段落 `<p>####### foo</p>` ✓
- Ex64 `#5 bolt`/`#hashtag` → `#` 后非空白 → null → 段落 ✓
- Ex68 ` ### foo`/`  ## foo`/`   # foo` → 跳 1/2/3 空格 → h3/h2/h1 ✓
- Ex71 `## foo ##`→`foo`(闭合前是空格);`  ###   bar    ###`→跳2空格+闭合→`bar` ✓
- Ex75 `# foo#` → 尾 `#` 前是 `o`（非空格）→ 不剥 → `foo#` ✓
- Ex76 `### foo \###` → 尾 `###` 前是 `\`（非空格）→ 不剥 → `foo \###` → inline `\#`→`#` → `foo ###` ✓

## ⚠️ ArkTS 严格模式（必守，harness 不查，会炸 DevEco）
- `AtxOpen` 用**具名 class + new**，禁匿名对象字面量 `{...}`。
- 返回 `AtxOpen | null` 联合类型合法。
- **无正则**；用字符循环 / `substring`。
- `line[i]`/`s[i]` 字符串下标读取合法；禁对象属性 `obj[key]` 索引、Record、spread、any。
- import 全在顶部（无新 import，parseInlines 已可用）。

## 不做（明确不碰，控回归）
- **段落边界检测**（`setsParaOpen`/`isLazyContinuable`/`parseParagraph`/`isNonListBlockStart`）的 `startsWith('#')` **保持不动**——本批 6 例均为块级独立标题行，无「缩进标题打断段落」场景。改它牵涉 Setext/Block quote 血缘，本批不碰。
- Indented code（Ex111/117 空白行归一化）、Ex108（列表内容模型）——非 ATX，另批。

## 验收 gate（Hermes 亲跑核对，不信 CC 自报）
1. **ATX headings 12/18 → 18/18**。
2. **总 exact ≥ 567 且上升**。
3. **全节回归对比**（`git show HEAD:tools/commonmark-spec/baseline.json` vs 新跑）——**REGRESSIONS NONE** + **逐节净增量 = 总增量**自洽。重点盯：Thematic breaks（`---`/`###` 边界）、Setext headings、Paragraphs、Block quotes。
4. error=0、hang=0、cosmetic=0。
5. ArkTS grep 0 命中（匿名对象字面量 / Record / spread / any / 新正则 / 对象属性下标）。

## 必查样例（全 PASS 才算过）
Ex63, 64, 68, 71, 75, 76（ATX 全 6 条）。
