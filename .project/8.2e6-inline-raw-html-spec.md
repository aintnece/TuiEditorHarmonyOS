# 8.2e-6 — inline Raw HTML（行内原始 HTML 标签）

## 目标

让解析器识别 **CommonMark 行内原始 HTML**（开标签/闭标签/注释/处理指令/声明/CDATA），原样透传不转义。新增 `HtmlInline` 节点类型。
当前 Raw HTML 节 6/20。目标翻 Ex613/614/615/616/617/625/626 等 + 连带 Emphasis 剩的 Ex475/476/477、Hard breaks 剩的 Ex642/643（这些是 `<...>` 被错转义导致的）。**全节 0 回归**。

> ⚠️ Ex623/624（`</a></foo >`、`</a href="foo">`）当前被**块级 HTML** 吞（actual 无 `<p>` 包裹），可能不靠本 inline 批翻——**只要不回归即可**，不在必过。

## 改三个文件

1. **`Node.ts`**：`AstNodeType` 枚举加一项 `HtmlInline = 'htmlInline'`。
2. **`html/Renderer.ts`**：渲染分发加 `case AstNodeType.HtmlInline: return node.text;`（**逐字输出，不转义**，与 HtmlBlock 同理）。
3. **`commonmark/Inlines.ts`**：在 `<` 分支（autolink 判定**之后**）加 `tryParseHtmlTag` 引号感知扫描；命中则产 `HtmlInline` 节点（text=原始子串）。新增 1 个主函数 + 6 个产生式解析 + 若干 charCode helper。

- **不要**动其它分支、8.2b~8.2e5 代码、`Blocks.ts`/`LinkRefs.ts`/其它文件。
- **不要**新增正则；全部 charCode + 字符串索引/`indexOf`。复用已有 `isAsciiLetter`/`isAsciiDigit`/`isAsciiAlnum`。

---

## 一、CommonMark 行内 HTML 文法（对标 commonmark.js reHtmlTag，0.31.2）

- **TAGNAME** = `[A-Za-z][A-Za-z0-9-]*`
- **ATTRIBUTENAME** = `[A-Za-z_:][A-Za-z0-9_.:-]*`
- **属性值**：无引号 `[^"'=<>`+空白+控制符]+` | 单引号 `'...'` | 双引号 `"..."`（**引号内可含 `>`/`<`！** Ex616）
- **属性** = 前置空白(含换行) + 属性名 + 可选(`\s*=\s*` + 值)
- **开标签** = `<` TAGNAME 属性* `\s*` `/?` `>`（标签可跨行，空白含 `\n`）
- **闭标签** = `</` TAGNAME `\s*` `>`（**不许属性**；Ex624 `</a href=...>` 非法→字面）
- **注释** = `<!-->` | `<!--->` | `<!--` + (不含 `-->` 的任意字符) + `-->`（0.31.2，允许内部 `--`）
- **处理指令** = `<?` + 任意(含换行,惰性) + `?>`
- **声明** = `<!` + ASCII 字母 + (任意非 `>`) + `>`
- **CDATA** = `<![CDATA[` + 任意(含换行) + `]]>`

> 透传规则：命中的整段原文（含 `<` `>`）原样进 `HtmlInline.text`，renderer **不转义**输出。所以 `<img title="*"/>` 里的 `*` 被整段吞掉、**不参与强调**（Ex475 因此转正）。

---

## 二、Inlines.ts：`<` 分支接入（autolink 之后）

现有 `<` 分支（约 247–272 行）：先 `isUriAutolink` 再 `isEmailAutolink`。**在两者之后、`}` 之前**加：

```typescript
    // ── 行内原始 HTML 标签 ──
    const htmlEnd: number = tryParseHtmlTag(text, i);
    if (htmlEnd > i) {
      const node: AstNode = new AstNode(AstNodeType.HtmlInline);
      node.text = text.substring(i, htmlEnd);
      out.push(node);
      i = htmlEnd;
      continue;
    }
```

> autolink 用 `indexOf('>')`+校验、raw HTML 用独立引号感知扫描，互不影响：autolink 先试（`<scheme:..>`/`<email>`），不成立再试 HTML 标签，再不成立 `<` 落普通文本→`&lt;`。

---

## 三、Inlines.ts：tryParseHtmlTag + 产生式解析（放 helper 区）

```typescript
/** HTML 空白：空格/Tab/换行/回车/换页 */
function isHtmlWs(ch: string): boolean {
  return ch === ' ' || ch === '\t' || ch === '\n' || ch === '\r' || ch === '\f';
}
function isAttrNameStart(c: number): boolean {
  return isAsciiLetter(c) || c === 95 /*_*/ || c === 58 /*:*/;
}
function isAttrNameChar(c: number): boolean {
  return isAsciiAlnum(c) || c === 95 /*_*/ || c === 58 /*:*/ || c === 46 /*.*/ || c === 45 /*-*/;
}
/** 无引号属性值字符：非 " ' = < > ` 且非空白/控制符（码点 > 0x20） */
function isUnquotedValueChar(c: number): boolean {
  if (c <= 0x20) return false;
  return c !== 34 /*"*/ && c !== 39 /*'*/ && c !== 61 /*=*/ &&
         c !== 60 /*<*/ && c !== 62 /*>*/ && c !== 96 /*`*/;
}
function skipHtmlWs(text: string, p: number): number {
  while (p < text.length && isHtmlWs(text[p])) { p++; }
  return p;
}

/** 行内 HTML 标签：从 start('<') 解析，返回标签后第一个索引；非法返回 -1 */
function tryParseHtmlTag(text: string, start: number): number {
  const len: number = text.length;
  if (start >= len || text[start] !== '<') return -1;
  if (start + 1 >= len) return -1;
  const c1: string = text[start + 1];
  if (c1 === '!') {
    // 注释 <!-- / CDATA <![CDATA[ / 声明 <!Letter
    if (start + 3 < len && text[start + 2] === '-' && text[start + 3] === '-') {
      return parseHtmlComment(text, start);
    }
    if (start + 8 < len && text.substring(start + 2, start + 9) === '[CDATA[') {
      return parseHtmlCdata(text, start);
    }
    return parseHtmlDeclaration(text, start);
  }
  if (c1 === '?') return parseHtmlPI(text, start);
  if (c1 === '/') return parseHtmlCloseTag(text, start);
  return parseHtmlOpenTag(text, start);
}

/** 开标签 <tag attr* /?> */
function parseHtmlOpenTag(text: string, start: number): number {
  const len: number = text.length;
  let p: number = start + 1;
  if (p >= len || !isAsciiLetter(text.charCodeAt(p))) return -1;
  p++;
  while (p < len && (isAsciiAlnum(text.charCodeAt(p)) || text[p] === '-')) { p++; }
  // 属性循环
  while (p < len) {
    if (isHtmlWs(text[p])) {
      p = skipHtmlWs(text, p);
      if (p < len && isAttrNameStart(text.charCodeAt(p))) {
        p++;
        while (p < len && isAttrNameChar(text.charCodeAt(p))) { p++; }
        // 可选 = 值
        let vp: number = skipHtmlWs(text, p);
        if (vp < len && text[vp] === '=') {
          vp = skipHtmlWs(text, vp + 1);
          if (vp >= len) return -1;
          const q: string = text[vp];
          if (q === '\'') {
            const e: number = text.indexOf('\'', vp + 1);
            if (e < 0) return -1;
            p = e + 1;
          } else if (q === '"') {
            const e: number = text.indexOf('"', vp + 1);
            if (e < 0) return -1;
            p = e + 1;
          } else {
            const s2: number = vp;
            while (vp < len && isUnquotedValueChar(text.charCodeAt(vp))) { vp++; }
            if (vp === s2) return -1;
            p = vp;
          }
        }
        continue;   // 该属性消费完，继续找下一个
      } else {
        break;      // 空白后非属性名 → 结尾空白，去判 /?>
      }
    } else {
      break;        // 无空白 → 直接判 /?>
    }
  }
  if (p < len && text[p] === '/') { p++; }
  if (p < len && text[p] === '>') return p + 1;
  return -1;
}

/** 闭标签 </tag > （不许属性） */
function parseHtmlCloseTag(text: string, start: number): number {
  const len: number = text.length;
  let p: number = start + 2;   // 跳过 </
  if (p >= len || !isAsciiLetter(text.charCodeAt(p))) return -1;
  p++;
  while (p < len && (isAsciiAlnum(text.charCodeAt(p)) || text[p] === '-')) { p++; }
  p = skipHtmlWs(text, p);
  if (p < len && text[p] === '>') return p + 1;
  return -1;
}

/** 注释 */
function parseHtmlComment(text: string, start: number): number {
  const len: number = text.length;
  let p: number = start + 4;   // 跳过 <!--
  if (p < len && text[p] === '>') return p + 1;                       // <!-->
  if (p + 1 < len && text[p] === '-' && text[p + 1] === '>') return p + 2;  // <!--->
  while (p + 2 < len) {
    if (text[p] === '-' && text[p + 1] === '-' && text[p + 2] === '>') return p + 3;
    p++;
  }
  return -1;
}

/** 处理指令 <? ... ?> */
function parseHtmlPI(text: string, start: number): number {
  const len: number = text.length;
  let p: number = start + 2;
  while (p + 1 < len) {
    if (text[p] === '?' && text[p + 1] === '>') return p + 2;
    p++;
  }
  return -1;
}

/** 声明 <!Letter ... > */
function parseHtmlDeclaration(text: string, start: number): number {
  const len: number = text.length;
  let p: number = start + 2;   // 跳过 <!
  if (p >= len || !isAsciiLetter(text.charCodeAt(p))) return -1;
  p++;
  while (p < len && text[p] !== '>') { p++; }
  if (p < len && text[p] === '>') return p + 1;
  return -1;
}

/** CDATA <![CDATA[ ... ]]> */
function parseHtmlCdata(text: string, start: number): number {
  const len: number = text.length;
  let p: number = start + 9;   // 跳过 <![CDATA[
  while (p + 2 < len) {
    if (text[p] === ']' && text[p + 1] === ']' && text[p + 2] === '>') return p + 3;
    p++;
  }
  return -1;
}
```

> 注意 charCode 用 `text.charCodeAt(p)`，单字符比较用 `text[p] === '...'`，均合规。`text.indexOf('"', vp+1)` 找引号闭合（引号内的 `>` 因此被跳过——Ex616 关键）。

---

## 四、Node.ts / Renderer.ts 改动

**Node.ts** 枚举（在 `Strike` 附近）加：
```typescript
  HtmlInline = 'htmlInline',
```

**Renderer.ts** 渲染分发（在 `case AstNodeType.Code:` 附近）加：
```typescript
      case AstNodeType.HtmlInline: return node.text;
```

---

## 五、ArkTS 硬约束

- 禁匿名对象字面量/`Record`/索引/spread/`any`/**新正则**（全 charCode + 字符串索引 + `indexOf`）。
- `tryParseHtmlTag` 等返回 `number`（索引），不返回对象。
- 复用 `isAsciiLetter`/`isAsciiDigit`/`isAsciiAlnum`，勿重定义。
- 自检：本批不新增 `new RegExp`/`.replace(/`；匿名对象字面量仍 0。

---

## 六、必过样例

Raw HTML：
- Ex613 `<a><bab><c2c>` → `<p><a><bab><c2c></p>`
- Ex614 `<a/><b2/>` → `<p><a/><b2/></p>`
- Ex615 `<a  /><b2␤data="foo" >` → 跨行属性，原样
- Ex616 `<a foo="bar" bam = 'baz <em>"</em>' ...>` → 单引号值含 `<em>`/`>` 原样（引号感知）
- Ex617 `Foo <responsive-image src="foo.jpg" />` → 连字符标签名
- Ex625 `foo <!-- this is a --␤comment - with hyphens -->` → 注释含 `--`、跨行
- Ex626 `foo <!--> foo -->` / `foo <!---> foo -->` → `<!-->`/`<!--->` 特例

连带（`<...>` 此前被错转义）：
- Emphasis Ex475 `*<img src="foo" title="*"/>` → `<p>*<img src="foo" title="*"/></p>`（`*` 在标签内不成强调）
- Emphasis Ex476 `**<a href="**">` → `<p>**<a href="**"></p>`
- Emphasis Ex477 `__<a href="__">` → `<p>__<a href="__"></p>`
- Hard breaks Ex642 `<a href="foo  ␤bar">` / Ex643 `<a href="foo\␤bar">` → 标签内空白/反斜杠原样

> ⚠️ 预期**不翻**（只要不回归）：Ex623/624（被块级 HTML 吞，属 Blocks 层）。

---

## 七、运行 + 验收

```bash
cd /data/docs/TuiEditorHarmonyOS/tools/commonmark-spec && ./node_modules/.bin/tsx run-spec.ts
```
报告：总 exact（基线 **463**）、Raw HTML 节（基线 **6/20**）、Emphasis 节（应 129→132 满分）、Hard line breaks 节、各分节增减、error/hang（必 0），第六节必过样例逐条 actual 是否 = expected，并确认 Ex623/624 与已通过用例不回归。

## 八、CC 完成后报告

- 三文件改动清单（Node.ts 枚举、Renderer.ts case、Inlines.ts 主函数+6 产生式+helper）。
- harness 前后：总 exact、Raw HTML、Emphasis、Hard line breaks、各节增减、error/hang。
- 第六节样例逐条结果 + 确认 Ex623/624 与已通过用例不回归；未动其它文件、未加正则。
- 不要 git commit/push。
