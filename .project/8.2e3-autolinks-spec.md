# 8.2e-3 — Autolinks（`<scheme:uri>` / `<email>` 严格识别）

## 目标

把 `entry/src/main/ets/parser/commonmark/Inlines.ts` 的自动链接从**粗糙 `isAutolink`（只认 http/https/ftp/mailto + 任意 `@`）**换成 **CommonMark 0.31.2 标准识别**：URI autolink（scheme 文法校验）+ email autolink（严格校验 + `mailto:` 前缀）+ 拒绝空格/控制符。
当前 Autolinks 节 10/19，目标 +8（Ex596/598/599/601/602/604/605/606），**全节 0 回归**。

> ⚠️ **Ex603 不在本批**：`<https://example.com/\[\>` 期望 href 百分号编码成 `%5C%5B%5C`（CommonMark normalizeURI / mdurl.encode）。该百分号编码是会同时影响 Links/Images/Link-ref 的**横切功能**，单独成批做（本批只设 `url=inner` 原文，renderer 仍做 HTML escape）。Ex603 本批保持现状即可（不回归）。

## 只改一个文件

- **`entry/src/main/ets/parser/commonmark/Inlines.ts`** —— 仅改：autolink 分支（现 246–262 行）+ 重写 `isAutolink`（现 829–838 行）→ 拆成两个 char-loop 校验函数。
- **不要**动其它分支、8.2b `Delimiter`/`EmphasisProcessor`、8.2e-1 code span、8.2e-2 entity；**不要**碰 `Blocks.ts`/`Renderer.ts`/其它文件。
- **不要**新增正则；scheme/email 校验全部用 charCode 字符循环。
- **不要**碰 link destination 的 URL 编码（normalizeURI 留横切批次）。

---

## 一、现状（要替换）

**autolink 分支（246–262 行）**：裸 `indexOf('>')` + 窄 `isAutolink`，把 `node.attrs.url = inner`、text=inner。问题：① 不认任意合法 scheme；② 不拒绝含空格 URI；③ email 不加 `mailto:`；④ 不拒非法 email。

**`isAutolink`（829–838 行）**：只 `startsWith('http://'|'https://'|'ftp://'|'mailto:')` 或 `indexOf('@')>0`。

---

## 二、CommonMark Autolink 规则

**URI autolink** `<scheme:rest>`：
- **scheme** = 首字符 ASCII 字母，其后 1–31 个 `[A-Za-z0-9+.-]`，总长 **2–32**；scheme 之后紧跟 `:`。
- `:` 之后的 `rest` = 任意字符，但**不得含** ASCII 空白、`<`、控制符（码点 ≤ 0x1F 或 = 0x7F）。（`>` 天然不会出现，因为 inner 取到第一个 `>` 为止。）
- 命中 → `<a href="URI">URI</a>`：`url = inner`（原文，renderer 做 HTML escape），text = inner。

**Email autolink** `<email>`（URI autolink 不成立时再试）：
- 严格匹配（对标 commonmark 的 email 正则，用字符循环实现）：
  - **local part**：1+ 个 `[A-Za-z0-9.!#$%&'*+/=?^_`{|}~-]`
  - 一个 `@`
  - **domain**：1+ 个 label，label 间用 `.` 分隔；每个 label = `[A-Za-z0-9]` 开头、`[A-Za-z0-9]` 结尾、中间 0–61 个 `[A-Za-z0-9-]`（单字符 label 合法）。
- 命中 → `<a href="mailto:email">email</a>`：`url = 'mailto:' + inner`，text = inner。

**都不成立** → `<` 留字面（分支不命中，落普通文本，renderer escape 成 `&lt;`）。

---

## 三、重写：两个 char-loop 校验函数（替换 `isAutolink`）

```typescript
function isAsciiLetter(c: number): boolean { return (c >= 65 && c <= 90) || (c >= 97 && c <= 122); }
function isAsciiDigit(c: number): boolean { return c >= 48 && c <= 57; }
function isAsciiAlnum(c: number): boolean { return isAsciiLetter(c) || isAsciiDigit(c); }

/** URI autolink：合法返回 true（inner = <...> 之间的内容） */
function isUriAutolink(inner: string): boolean {
  const n: number = inner.length;
  if (n < 1) return false;
  // scheme：首字母
  if (!isAsciiLetter(inner.charCodeAt(0))) return false;
  let p: number = 1;
  while (p < n) {
    const c: number = inner.charCodeAt(p);
    if (isAsciiAlnum(c) || c === 43 /*+*/ || c === 46 /*.*/ || c === 45 /*-*/) { p++; }
    else { break; }
  }
  // p 处必须是 ':'，scheme 长度 2–32
  if (p >= n || inner[p] !== ':') return false;
  if (p < 2 || p > 32) return false;
  // rest（: 之后）不得含空白 / < / 控制符
  for (let k: number = p + 1; k < n; k++) {
    const c: number = inner.charCodeAt(k);
    if (c <= 0x20 || c === 0x7F) return false;   // 含空格(0x20)、tab、控制符
    if (c === 60 /*<*/) return false;
  }
  return true;
}

/** Email autolink：合法返回 true */
function isEmailAutolink(inner: string): boolean {
  const n: number = inner.length;
  const at: number = inner.indexOf('@');
  if (at <= 0 || at >= n - 1) return false;       // 必须有 @ 且两侧非空
  // local part：1+ 个允许字符（无 @ 之前再次出现 @ 已由 indexOf 第一个 @ 界定，但 local 内不能有 @）
  for (let k: number = 0; k < at; k++) {
    const c: number = inner.charCodeAt(k);
    const ok: boolean = isAsciiAlnum(c) ||
      c === 46 || c === 33 || c === 35 || c === 36 || c === 37 || c === 38 ||  // . ! # $ % &
      c === 39 || c === 42 || c === 43 || c === 47 || c === 61 || c === 63 ||  // ' * + / = ?
      c === 94 || c === 95 || c === 96 || c === 123 || c === 124 || c === 125 || // ^ _ ` { | }
      c === 126 || c === 45;                                                    // ~ -
    if (!ok) return false;
  }
  // domain：label('.'label)*，label = alnum (alnum|-){0,61} 末位 alnum
  const domain: string = inner.substring(at + 1);
  const labels: string[] = domain.split('.');
  for (let li: number = 0; li < labels.length; li++) {
    const lab: string = labels[li];
    const m: number = lab.length;
    if (m < 1 || m > 63) return false;
    if (!isAsciiAlnum(lab.charCodeAt(0))) return false;
    if (!isAsciiAlnum(lab.charCodeAt(m - 1))) return false;
    for (let j: number = 1; j < m - 1; j++) {
      const c: number = lab.charCodeAt(j);
      if (!(isAsciiAlnum(c) || c === 45 /*-*/)) return false;
    }
  }
  return true;
}
```

> `String.split('.')` 在 ArkTS 合规（无正则，字符串分隔）。`inner.indexOf('@')` 取第一个 `@`：local 部分循环里不允许 `@`（不在允许集），所以多 `@` 会被 local 校验拒掉。

## 四、重写 autolink 分支（246–262 行）

```typescript
// ── 自动链接 <scheme:uri> / <email> ──
if (text[i] === '<') {
  const end: number = text.indexOf('>', i + 1);
  if (end >= 0) {
    const inner: string = text.substring(i + 1, end);
    if (isUriAutolink(inner)) {
      const node: AstNode = new AstNode(AstNodeType.Link);
      node.attrs.url = inner;                 // 原文（normalizeURI 留横切批次）；renderer 做 HTML escape
      const txt: AstNode = new AstNode(AstNodeType.Text);
      txt.text = inner;
      node.appendChild(txt);
      out.push(node);
      i = end + 1;
      continue;
    }
    if (isEmailAutolink(inner)) {
      const node: AstNode = new AstNode(AstNodeType.Link);
      node.attrs.url = 'mailto:' + inner;
      const txt: AstNode = new AstNode(AstNodeType.Text);
      txt.text = inner;
      node.appendChild(txt);
      out.push(node);
      i = end + 1;
      continue;
    }
  }
}
```

> 去掉旧 `i + 2 < len` 守卫（`indexOf` 自查）。删除旧 `isAutolink`（被两新函数取代）。

---

## 五、ArkTS 硬约束

- 禁匿名对象字面量（本批不需返回对象）。
- 禁 `Record`/对象索引/spread/`any`/`unknown`/**新正则**（scheme/email 全 charCode + `split('.')`）。
- 数组下标 `labels[li]`、字符串取字符 `inner[p]` / `inner.charCodeAt(k)` 合规。
- 自检：`grep -nE "new RegExp|\.replace\(/|/\^" Inlines.ts` 本批不新增；匿名对象字面量仍 0。

---

## 六、必过样例（实现后转正，共 8 条）

- Ex596 `<irc://foo.bar:2233/baz>` → `<a href="irc://foo.bar:2233/baz">irc://foo.bar:2233/baz</a>`
- Ex598 `<a+b+c:d>` → `<a href="a+b+c:d">a+b+c:d</a>`
- Ex599 `<made-up-scheme://foo,bar>` → `<a href="made-up-scheme://foo,bar">…</a>`
- Ex601 `<localhost:5001/foo>` → `<a href="localhost:5001/foo">…</a>`
- Ex602 `<https://foo.bar/baz bim>` → `<p>&lt;https://foo.bar/baz bim&gt;</p>`（含空格 → 非 autolink，留字面）
- Ex604 `<foo@bar.example.com>` → `<a href="mailto:foo@bar.example.com">foo@bar.example.com</a>`
- Ex605 `<foo+special@Bar.baz-bar0.com>` → `<a href="mailto:foo+special@Bar.baz-bar0.com">…</a>`
- Ex606 `<foo\+@bar.example.com>` → `<p>&lt;foo+@bar.example.com&gt;</p>`（反斜杠非法 email → 留字面；注意输入里 `\+` 是转义分支先处理成 `+`，autolink 看到的 inner 已不含反斜杠但分支顺序上 `<` 先到——以 spec.json 期望为准，保持不回归即可）

> ⚠️ 预期**不翻**（不在本批，只要不回归）：Ex603（href 需 normalizeURI 百分号编码，留横切批次）。
> ⚠️ **不得回归**已通过的 ~10 条 autolink（含 `<http://...>`/`<https://...>`/`<mailto:...>`/已知非 autolink 的 `<` 用例）——新 scheme 校验是旧窄判定的超集。

---

## 七、运行 + 验收

```bash
cd /data/docs/TuiEditorHarmonyOS/tools/commonmark-spec && ./node_modules/.bin/tsx run-spec.ts
```
报告：总 exact（基线 **441**）、Autolinks 节（基线 **10/19**）、Raw HTML 节（确认不回归，`<` 相关）、各分节增减、error/hang（必 0），第六节 8 条必过样例逐条 actual 是否 = expected。

## 八、CC 完成后报告

- 改了哪个分支 + 新增/删除哪些函数（应只在 `Inlines.ts`，删 `isAutolink`、加 `isUriAutolink`/`isEmailAutolink`/`isAsciiLetter`/`isAsciiDigit`/`isAsciiAlnum`）。
- harness 前后：总 exact、Autolinks 节、各节增减、error/hang。
- 第六节 8 条逐条结果 + 确认 Ex603 与已通过 autolink 不回归；未动其它分支/文件、未加正则。
- 不要 git commit/push。
