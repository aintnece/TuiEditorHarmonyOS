# 8.2e-2 — Entity 数字字符引用（十进制 + 十六进制 numeric refs）

## 目标

把 `entry/src/main/ets/parser/commonmark/Inlines.ts` 的实体解析里**数字引用返回占位符 `'?'`** 的桩实现，换成 **CommonMark 0.31.2 标准数字字符引用解码**（十进制 `&#NN;` + 十六进制 `&#xHH;`/`&#XHH;` + 合法性规则 + U+FFFD 替换 + 真解码 `String.fromCodePoint`）。顺带把脆弱的「rough estimate」推进逻辑修干净。
当前 Entity 节 0/12，目标 +7 左右（Ex26/27/28/37/38/39/40），**全节 0 回归**。命名实体大表（Ex25/32/33/34）拆给后续批次，本批**不做**。

## 只改一个文件

- **`entry/src/main/ets/parser/commonmark/Inlines.ts`** —— 仅改：实体分支（现 281–294 行）+ `tryParseEntity` 函数（现 838–854 行）+ 新增 1 个具名类 `EntityResult`。
- **不要**动其它分支、8.2b 的 `Delimiter`/`EmphasisProcessor`、8.2e-1 的 code span helper、`Blocks.ts`/`Renderer.ts`/其它文件。
- **不要**新增正则；数字判定用 charCode；解码用 `parseInt(str, radix)` + `String.fromCodePoint(cp)`。
- **保留**现有 6 个命名实体（`&amp; &lt; &gt; &quot; &apos; &nbsp;`），别删别改 → 0 回归。

---

## 一、现状（要替换）

**实体分支（281–294 行）**——推进逻辑是脆弱 hack（`i += entity.length + 2` 再 `indexOf(';')`）：
```typescript
if (text[i] === '&' && i + 2 < len) {
  const entity: string | null = tryParseEntity(text, i);
  if (entity) {
    const txt: AstNode = new AstNode(AstNodeType.Text);
    txt.text = entity;
    out.push(txt);
    i += entity.length + 2; // rough estimate
    const semi: number = text.indexOf(';', i);
    if (semi >= 0) { i = semi + 1; }
    continue;
  }
}
```

**`tryParseEntity`（838–854 行）**——数字分支直接返回 `'?'`（Ex26/27/37/38/39/40 输出 `?` 的根因）：
```typescript
function tryParseEntity(text: string, start: number): string | null {
  const semi: number = text.indexOf(';', start);
  if (semi < 0 || semi - start > 20) return null;
  const entity: string = text.substring(start, semi + 1);
  if (entity === '&amp;') return '&';
  ... 6 个命名 ...
  if (entity.startsWith('&#')) {
    return '?'; // 简化：占位符
  }
  return null;
}
```

---

## 二、CommonMark 数字引用规则

- **十进制**：`&#` + **1–7 位十进制数字** + `;` → 码点 N。
- **十六进制**：`&#x` 或 `&#X` + **1–6 位十六进制数字** + `;` → 码点 N。
- **U+FFFD 替换**：若 N === 0，或 N > 0x10FFFF，或 N 落在代理区 0xD800–0xDFFF → 解码成 U+FFFD（`\uFFFD`，`�`）。其余直接 `String.fromCodePoint(N)`。
- **非法 → 留字面**（返回 null，让分支回退把 `&` 当普通文本，renderer escape 成 `&amp;`）：
  - 无数字（`&#;` / `&#x;`）
  - 数字超长（十进制 >7 位，如 `&#87654321;`；十六进制 >6 位）
  - 十进制位里有非数字（`&#abcdef0;`）
  - 缺结尾 `;`
- 解码出的字符进 **Text 节点**，**不**再被当作 Markdown 标记（如 `&#42;`→`*` 是字面星号，不参与强调；因为它在 Text 节点里、不入 delimiter 栈——本就如此，无需特殊处理）。

---

## 三、新增具名类（ArkTS 禁匿名对象字面量）

```typescript
/** 实体解析结果：解码值 + 消费到的下一位置 */
class EntityResult {
  value: string = '';
  nextIdx: number = 0;   // 实体之后第一个字符的索引（即 ';' 之后）
}
```

## 四、重写 `tryParseEntity`（返回 `EntityResult | null`）

```typescript
/** 是否十进制数字字符 */
function isDecimalDigit(ch: string): boolean {
  const c: number = ch.charCodeAt(0);
  return c >= 48 && c <= 57;                      // 0-9
}

/** 是否十六进制数字字符 */
function isHexDigit(ch: string): boolean {
  const c: number = ch.charCodeAt(0);
  return (c >= 48 && c <= 57) ||                  // 0-9
         (c >= 65 && c <= 70) ||                  // A-F
         (c >= 97 && c <= 102);                   // a-f
}

/** 实体引用解码：命名(6 个) + 数字(十进制/十六进制)。返回 EntityResult 或 null(非实体，留字面) */
function tryParseEntity(text: string, start: number): EntityResult | null {
  const len: number = text.length;
  if (start >= len || text[start] !== '&') return null;

  // ── 数字引用 &#... ──
  if (start + 1 < len && text[start + 1] === '#') {
    let p: number = start + 2;
    let isHex: boolean = false;
    if (p < len && (text[p] === 'x' || text[p] === 'X')) { isHex = true; p++; }
    const digitStart: number = p;
    while (p < len && (isHex ? isHexDigit(text[p]) : isDecimalDigit(text[p]))) { p++; }
    const numDigits: number = p - digitStart;
    // 合法性
    if (numDigits < 1) return null;
    if (isHex && numDigits > 6) return null;
    if (!isHex && numDigits > 7) return null;
    if (p >= len || text[p] !== ';') return null;
    // 解析码点
    const digits: string = text.substring(digitStart, p);
    let cp: number = parseInt(digits, isHex ? 16 : 10);
    if (cp === 0 || cp > 0x10FFFF || (cp >= 0xD800 && cp <= 0xDFFF)) {
      cp = 0xFFFD;
    }
    const er: EntityResult = new EntityResult();
    er.value = String.fromCodePoint(cp);
    er.nextIdx = p + 1;                            // 跳过 ';'
    return er;
  }

  // ── 命名实体（保留现有 6 个）──
  const semi: number = text.indexOf(';', start);
  if (semi < 0 || semi - start > 32) return null;
  const entity: string = text.substring(start, semi + 1);
  let v: string | null = null;
  if (entity === '&amp;') { v = '&'; }
  else if (entity === '&lt;') { v = '<'; }
  else if (entity === '&gt;') { v = '>'; }
  else if (entity === '&quot;') { v = '"'; }
  else if (entity === '&apos;') { v = "'"; }
  else if (entity === '&nbsp;') { v = ' '; }
  if (v === null) return null;
  const er2: EntityResult = new EntityResult();
  er2.value = v;
  er2.nextIdx = semi + 1;
  return er2;
}
```

> `String.fromCodePoint` / `parseInt(str, radix)` 是标准 ES。若 DevEco 对 `String.fromCodePoint` 报不支持（不太可能），再退化为 BMP 用 `String.fromCharCode`、astral 用代理对计算——但优先用 `fromCodePoint`。

## 五、改实体分支（281–294 行 → 用 EntityResult，去掉 rough hack）

```typescript
// ── 实体引用 &amp; &#NN; &#xHH; 等 ──
if (text[i] === '&') {
  const er: EntityResult | null = tryParseEntity(text, i);
  if (er !== null) {
    const txt: AstNode = new AstNode(AstNodeType.Text);
    txt.text = er.value;
    out.push(txt);
    i = er.nextIdx;
    continue;
  }
}
```

> 去掉原 `i + 2 < len` 守卫（`tryParseEntity` 自己查边界）和脆弱的 `indexOf` 推进。`&` 不成实体时回退到普通文本分支（`text[i]`='&' 入 Text，`i++`），renderer escape 成 `&amp;`，与原行为一致。

---

## 六、ArkTS 硬约束

- 禁匿名对象字面量 → `EntityResult` 具名 class + `new` + 字段赋值。
- 禁 `Record`/对象索引/spread/`any`/`unknown`/新正则。
- `text[i]` 字符串取字符合规。
- 自检：`grep -nE "new RegExp|\.replace\(/" Inlines.ts` 本批不新增；`grep -nE "return \{|: *\{ *[a-z_]+:" Inlines.ts` 匿名对象字面量仍 0。

---

## 七、必过样例（实现后转正）

- Ex26 `&#35; &#1234; &#992; &#0;` → `<p># Ӓ Ϡ �</p>`（`&#0;` → U+FFFD）
- Ex27 `&#X22; &#XD06; &#xcab;` → `<p>&quot; ആ ಫ</p>`（大写 `&#X` 与小写 `&#x` 都认）
- Ex28 `&nbsp &x; &#; &#x;\n&#87654321;\n&#abcdef0;\n&ThisIsNotDefined; &hi?;` → 全留字面（数字非法部分不再输出 `?`；期望见 spec.json）
- Ex37 `&#42;foo&#42;\n*foo*` → `<p>*foo*\n<em>foo</em></p>`（`&#42;`→字面 `*`，不成强调）
- Ex38 `&#42; foo\n\n* foo` → `<p>* foo</p>` + `<ul><li>foo</li></ul>`
- Ex39 `foo&#10;&#10;bar` → `<p>foo\n\nbar</p>`（`&#10;`→LF）
- Ex40 `&#9;foo` → `<p>\tfoo</p>`（`&#9;`→tab）

> ⚠️ 预期**不翻**（不在本批，只要不回归）：Ex25/32/33/34（命名实体，需大表 + 链接 dest/title/info-string 上下文解码）、Ex31（raw HTML）。

---

## 八、运行 + 验收

```bash
cd /data/docs/TuiEditorHarmonyOS/tools/commonmark-spec && ./node_modules/.bin/tsx run-spec.ts
```
报告：总 exact（基线 **434**）、Entity 节（基线 **0/17** 左右，以 spec.json 为准）、各分节增减、error/hang（必 0），第七节必过样例逐条 actual 是否 = expected。

## 九、CC 完成后报告

- 改了哪个分支/函数 + 新增 `EntityResult` + 2 个 digit helper（应只在 `Inlines.ts`）。
- harness 前后：总 exact、Entity 节、各节增减、error/hang。
- 第七节样例逐条结果；确认保留了 6 个命名实体、未动其它分支/文件、未加正则。
- 不要 git commit/push。
