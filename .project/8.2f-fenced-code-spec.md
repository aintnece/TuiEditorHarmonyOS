# Phase 8.2f — Fenced code blocks（围栏代码块，CommonMark 4.5）

## 目标
修复 Fenced code blocks 节 15 条 STRUCT 失败（当前 14/29 → 目标 ≥27/29）。根因全部自包含在围栏代码块解析逻辑（`Blocks.ts` 的 `parseCodeBlock` + 入口检测）+ `Renderer.ts` 的 language class 渲染。**不碰列表/链接/内联/缩进代码块。**

## 现状（要替换的代码）

### `entry/src/main/ets/parser/commonmark/Blocks.ts`
- **入口检测（第 41 行）**：`if (line.startsWith('```') || line.startsWith('~~~')) return parseCodeBlock(state);`
  - 缺陷：① 不容忍 0-3 前导空格缩进；② 不校验「backtick 围栏的 info 不能含 backtick」（导致 `` ``` ``` `` 被误当围栏，应是行内 code span）。
- **`parseCodeBlock`（第 91-118 行）**：
  - `infoStart` 写死 3 → 开围栏 run 长度 ≠3 时把多余围栏字符吃进 info（Ex144 `language-\`;` 应为 `language-;`；Ex143 `language-~    ruby...`）。
  - 闭合判定 `line.startsWith(fenceChar+fenceChar+fenceChar)` 写死 3 个 → 不要求 run 长度 ≥ 开围栏长度（Ex124/125/127/139/147）、不要求闭合行仅尾随空白无内容（Ex147）、不允许闭合行 ≤3 前导空格（Ex135/136）。
  - 无前导缩进识别、无内容按开围栏缩进量去除（Ex131/132/133）。

### `entry/src/main/ets/parser/html/Renderer.ts`（第 166-168 行 `renderCodeBlock`）
```ts
const info: string = node.attrs.info;
const lang: string = info ? ' class="language-' + this.escapeHtml(info) + '"' : '';
```
- 缺陷：把**整个 info** 当 language class。CommonMark 规定 language = info 的**第一个空白分隔 token**（Ex143 info=`ruby startline=3 $%@#$` → `language-ruby`；Ex146 info=`aa \`\`\` ~~~` → `language-aa`）。

## CommonMark 规则（本批要落地的）
1. **开围栏**：≤3 前导空格 + ≥3 个连续 `` ` `` 或 `~`（= code fence）。记 `fenceChar`、`fenceLen`(run 长度)、`indent`(前导空格数)。
2. **info string**：开围栏 run 之后到行尾，`unescapeString(trim)`。**backtick 围栏的 info 不能含 `` ` ``**（含则整行不是围栏，回退普通段落 → 行内 code span 处理）；tilde 围栏 info 可含 `` ` ``。
3. **language class**：info 的第一个空白(空格/Tab)分隔 token。
4. **闭围栏**：≤3 前导空格 + 同 `fenceChar` 的连续 run，**run 长度 ≥ 开围栏 `fenceLen`**，其后**仅尾随空白**直到行尾（无 info/内容）。不满足则该行是内容。
5. **内容去缩进**：每行去掉**至多 `indent` 个前导空格**（实际不足则去现有的，部分去除 OK）。
6. 无闭围栏到 EOF 即结束（内容含到末尾）。

## 实现（只改 Blocks.ts + Renderer.ts，无正则，具名 class）

### Blocks.ts — 新增 helper（放 parseCodeBlock 上方或 helper 区）

```ts
/** 围栏开始信息（具名 class，禁匿名对象字面量） */
class FenceInfo {
  fenceChar: string = '';
  fenceLen: number = 0;
  indent: number = 0;
  info: string = '';
}

/** 解析开围栏行；不是合法围栏返回 null。
 *  规则：≤3 前导空格 + ≥3 同字符(` 或 ~) run；backtick 围栏 info 禁含 ` 。 */
function parseCodeFenceOpen(line: string): FenceInfo | null {
  let i: number = 0;
  while (i < line.length && line[i] === ' ' && i < 4) i++;
  const indent: number = i;
  if (indent >= 4) return null;            // ≥4 空格属缩进代码块，非围栏
  if (i >= line.length) return null;
  const ch: string = line[i];
  if (ch !== '`' && ch !== '~') return null;
  let runLen: number = 0;
  while (i < line.length && line[i] === ch) { runLen++; i++; }
  if (runLen < 3) return null;
  const rest: string = line.substring(i);
  if (ch === '`' && rest.indexOf('`') >= 0) return null;  // backtick 围栏 info 禁含 backtick
  const fi: FenceInfo = new FenceInfo();
  fi.fenceChar = ch;
  fi.fenceLen = runLen;
  fi.indent = indent;
  fi.info = unescapeString(rest.trim());
  return fi;
}

/** 入口用：是否是合法围栏开始 */
function isCodeFenceStart(line: string): boolean {
  return parseCodeFenceOpen(line) !== null;
}

/** 是否是匹配的闭围栏：≤3 空格 + 同 fenceChar run ≥ openLen + 其后仅空白 */
function isClosingFence(line: string, fenceChar: string, openLen: number): boolean {
  let i: number = 0;
  while (i < line.length && line[i] === ' ' && i < 4) i++;
  if (i >= 4) return false;                // ≥4 空格 → 内容，不是闭合
  let runLen: number = 0;
  while (i < line.length && line[i] === fenceChar) { runLen++; i++; }
  if (runLen < openLen) return false;
  return isAllWhitespaceFrom(line, i);     // 复用已有 helper（第 675 行）
}

/** 内容去缩进：去掉至多 n 个前导空格 */
function stripFenceIndent(line: string, n: number): string {
  let i: number = 0;
  while (i < line.length && i < n && line[i] === ' ') i++;
  return line.substring(i);
}
```

### Blocks.ts — 改入口（第 41 行）
```ts
// 代码块 (fenced)
if (isCodeFenceStart(line)) {
  return parseCodeBlock(state);
}
```

### Blocks.ts — 重写 parseCodeBlock（第 91-118 行）
```ts
export function parseCodeBlock(state: ParseState): AstNode {
  const node: AstNode = new AstNode(AstNodeType.CodeBlock);
  const fi: FenceInfo | null = parseCodeFenceOpen(state.currentLine());
  state.nextLine();
  // fi 入口已保证非 null；防御性处理
  const fenceChar: string = fi !== null ? fi.fenceChar : '`';
  const fenceLen: number = fi !== null ? fi.fenceLen : 3;
  const indent: number = fi !== null ? fi.indent : 0;
  node.attrs.info = fi !== null ? fi.info : '';

  let code: string = '';
  while (!state.isEnd()) {
    const line: string = state.currentLine();
    if (isClosingFence(line, fenceChar, fenceLen)) {
      state.nextLine();
      break;
    }
    code += stripFenceIndent(line, indent) + '\n';
    state.nextLine();
  }
  const textNode: AstNode = new AstNode(AstNodeType.Text);
  textNode.text = code;
  node.appendChild(textNode);
  return node;
}
```

### Renderer.ts — renderCodeBlock language 取第一 token（第 166-168 行）
```ts
private renderCodeBlock(node: AstNode): string {
  const info: string = node.attrs.info;
  // language class = info 第一个空白(空格/Tab)分隔 token
  let endTok: number = info.length;
  for (let k: number = 0; k < info.length; k++) {
    const c: string = info[k];
    if (c === ' ' || c === '\t') { endTok = k; break; }
  }
  const langWord: string = info.substring(0, endTok);
  const lang: string = langWord ? ' class="language-' + this.escapeHtml(langWord) + '"' : '';
  // ... 其余渲染逻辑保持不变（用 lang 拼 <pre><code...>）
}
```
**注意**：只改 language class 的取值方式（整 info → 第一 token），`<pre><code>` 内容部分一律不动。读完整 renderCodeBlock 再改，别动内容/换行处理。

## ⚠️ ArkTS 严格模式（必守，harness 不查，会炸 DevEco）
- `FenceInfo` 用**具名 class + new**，禁匿名对象字面量 `{...}`（8.2c-1 炸过）。
- 返回 `FenceInfo | null` 联合类型合法（参考已有 `blockQuoteMarkerStrip` 返回 `string|null`）。
- **无正则**（禁 `\\` 转义不可靠）；用字符循环 / `indexOf` / `startsWith` / `substring`。
- `line[i]` 字符串下标读取合法（已有代码大量使用）；禁对象属性 `obj[key]` 索引访问、禁 Record、禁 spread、禁 any。
- import 全在文件顶部；`unescapeString` / `isAllWhitespaceFrom` 已在 Blocks.ts 内可用（同文件），无需新 import。

## 验收 gate（Hermes 亲跑核对，不信 CC 自报）
1. **Fenced code blocks 节明显↑**（现 14/29 → 目标 ≥27/29）。
2. **总 exact ≥ 551 且上升**（现 551/652）。
3. **全节回归对比**（`git show HEAD:tools/commonmark-spec/baseline.json` vs 新跑 baseline.json）——**REGRESSIONS NONE**。重点盯：Indented code blocks（缩进边界相邻）、Backslash escapes（info unescape）、Code spans（Ex138/145 回退路径，应 +0 或 +2，不能 -）、Paragraphs。
4. error=0、hang=0、cosmetic=0。
5. ArkTS grep 0 命中（匿名对象字面量 / Record / spread / any / 新正则 / 对象属性下标）。

## 必查样例（全 PASS 才算过）
- 根因 A（run 长度+info）：Ex124, 125, 127, 139, 143, 144, 147
- backtick 围栏 info 禁含 backtick（回退 code span）：Ex138, 145
- tilde 围栏 info 取第一 token：Ex146
- 前导缩进 0-3 + 内容去缩进 + 闭合缩进：Ex131, 132, 133, 135, 136

## 不做（留后续）
- 围栏内容/闭合的 Tab 部分去除（commonmark.js 的 partiallyConsumedTab 列记账）——本批用 space-based 去缩进覆盖全部 15 例，Tab 边角留 Tabs 批次。
- isNonListBlockStart（第 580 行）仍用 `startsWith('```')`——本批 15 例无「缩进围栏打断段落/懒续行」场景，不动以控回归。
