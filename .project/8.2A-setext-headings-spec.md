# Phase 8.2-A — Setext headings（下划线式标题，CommonMark 4.3）

## 目标
修 Setext headings 节当前失败的 contained 样例。基线 exact **78.22% (510/652)**，Setext headings **20/27**。本批目标：Setext 节上升、全 26 节 0 回归。

## 只改一个文件
**`entry/src/main/ets/parser/commonmark/Blocks.ts`**。不动任何其它文件（不碰 Renderer/Node/Inlines/Gfm/ParseState）。

## 失败样例与根因（已 Hermes 分析，照修）

| Example | markdown | 期望 | 现状 bug | 本批修 |
|---------|----------|------|---------|--------|
| 84 | `  Foo\n  ===` | `<h1>Foo</h1>` | 下划线 `  ===` 有 2 前导空格，`isSetextUnderline` 要求 `line[0]` 即是 `=`/`-` → 拒绝 | 允许 0-3 前导空格 |
| 86 | `Foo\n   ----      ` | `<h2>Foo</h2>` | 3 前导空格 + 尾随空格被拒 → 退化成 `<hr>` | 允许 0-3 前导空格 + 尾随空白 |
| 88 | `Foo\n= =` 及 `Foo\n--- -` | `<p>Foo\n= =</p>` + `<p>Foo</p>\n<hr />` | `isSetextUnderline` 允许**空格穿插** → 把 `= =`/`--- -` 误判为下划线 | 改为连续 `=+`/`-+` 后仅尾随空白 |
| 82 | `  Foo *bar\nbaz*\t\n====` | `<h1>Foo <em>bar\nbaz</em></h1>` | 标题内容末尾 `\t` 未去 → `</em>\t</h1>` | setext 内容去尾随空白 |
| 89 | `Foo  \n-----` | `<h2>Foo</h2>` | 标题内容 `Foo  ` 尾随 2 空格未去 → `<h2>Foo  </h2>` | setext 内容去尾随空白 |
| 87 | `Foo\n    ---` | `<p>Foo\n---</p>` | `    ---`（4 空格缩进）应是段落懒续行，但 `isThematicBreak` 忽略缩进 → 误判 `<hr>` | `isThematicBreak` 加缩进≥4 守卫 |

**本批不修（排除，留后续）**：Example 93（`> foo\nbar\n===`）——需引用块**懒段落续行**（lazy continuation），属 blockquote 架构缺陷，非 setext 问题。本批只要它**不回归**（现状 actual 已错，保持现状即可）。

## 现有代码（要改的三处，Blocks.ts）

### 1) `isSetextUnderline`（约 653 行）—— 整个重写
```ts
// 现状（bug：不许前导空格、允许空格穿插）
function isSetextUnderline(line: string): boolean {
  if (line.length < 1) return false;
  const ch: string = line[0];
  if (ch !== '=' && ch !== '-') return false;
  for (let i = 0; i < line.length; i++) {
    if (line[i] !== ch && line[i] !== ' ') return false;   // ❌ 允许穿插空格
  }
  return true;
}
```
**改为**（返回 number：0=非下划线 / 1=H1(`=`) / 2=H2(`-`)；含 0-3 前导空格、连续 run、仅尾随空白）：
```ts
/** Setext 下划线检测：返回 0(非) / 1(=,H1) / 2(-,H2)。
 *  规则：0-3 前导空格 → 连续的 '='+ 或 '-'+ → 其后仅空格/制表到行尾。4+ 前导空格 → 0。 */
function setextUnderlineType(line: string): number {
  let i: number = 0;
  while (i < line.length && line[i] === ' ') { i += 1; }
  if (i > 3) return 0;                 // 4+ 前导空格 → 非下划线（缩进）
  if (i >= line.length) return 0;      // 全空白
  const ch: string = line[i];
  if (ch !== '=' && ch !== '-') return 0;
  // 连续 run
  let j: number = i;
  while (j < line.length && line[j] === ch) { j += 1; }
  // 其后只能是尾随空格/制表
  while (j < line.length) {
    const c: string = line[j];
    if (c !== ' ' && c !== '\t') return 0;
    j += 1;
  }
  return ch === '=' ? 1 : 2;
}
```
> 注意：旧函数名 `isSetextUnderline` 若无其它调用方可删除（grep 确认仅 parseParagraph 用）。直接用新的 `setextUnderlineType` 替换调用点。

### 2) parseParagraph 的 Setext 前瞻（约 429-440 行）
```ts
// 现状
if (!state.isEnd()) {
  const nextLine: string = state.currentLine();
  if (isSetextUnderline(nextLine)) {
    const heading: AstNode = new AstNode(AstNodeType.Heading);
    heading.attrs.level = nextLine.startsWith('=') ? 1 : 2;   // ❌ 有前导空格时 startsWith 失效
    parseInlines(text, heading);                               // ❌ text 未去尾随空白
    state.nextLine();
    return heading;
  }
}
```
**改为**：
```ts
if (!state.isEnd()) {
  const nextLine: string = state.currentLine();
  const setextLevel: number = setextUnderlineType(nextLine);
  if (setextLevel > 0) {
    const heading: AstNode = new AstNode(AstNodeType.Heading);
    heading.attrs.level = setextLevel;
    parseInlines(stripTrailingSpacesTabs(text), heading);   // 去标题内容尾随空白
    state.nextLine(); // 消费下划线行
    return heading;   // 直接返回，不返回段落
  }
}
```

### 3) `isThematicBreak`（约 665 行）—— 加缩进守卫
```ts
// 现状
export function isThematicBreak(line: string): boolean {
  const trimmed: string = line.trim();
  ...
}
```
**在函数体最前面加一行**（缩进≥4 列 → 不是分割线，按缩进代码/段落续行处理）：
```ts
export function isThematicBreak(line: string): boolean {
  if (countIndentColumns(line) >= 4) return false;   // 缩进≥4 → 非分割线
  const trimmed: string = line.trim();
  ...保持其余不变...
}
```
> `countIndentColumns` 已在 Blocks.ts（约 449 行）定义，直接调用。

### 4) 新增 helper（放 Blocks.ts 合适位置，如 isSetextUnderline 附近）
```ts
/** 去掉字符串尾随的空格/制表（不动行内、不动 \n）。 */
function stripTrailingSpacesTabs(s: string): string {
  let e: number = s.length;
  while (e > 0) {
    const c: string = s[e - 1];
    if (c !== ' ' && c !== '\t') break;
    e -= 1;
  }
  return s.substring(0, e);
}
```

## 逐例验证逻辑（实现后应满足）
- **84**：`  Foo` 段落 trimStart→`Foo`，前瞻 `  ===`→`setextUnderlineType`=1（2 前导空格 ≤3）→ `<h1>Foo</h1>`。
- **86**：前瞻 `   ----      `→ 3 前导空格 + `----` + 尾随空格 → type 2 → `<h2>Foo</h2>`。
- **88a**：`= =` → run `=` 后遇 `=`（非空白）→ type 0 → 非下划线 → `= =` 进段落 → `<p>Foo\n= =</p>`。
- **88b**：`--- -` → run `---` 后遇 `-`（非空白）→ type 0 → 段落 `Foo` 在下一轮被 `isThematicBreak('--- -')`(缩进0、全 `-`/空格) 打断 → `<p>Foo</p>` + `<hr />`。
- **82**：内容 `Foo *bar\nbaz*\t` → `stripTrailingSpacesTabs` 去末尾 `\t` → `Foo *bar\nbaz*` → `<h1>Foo <em>bar\nbaz</em></h1>`。
- **89**：内容 `Foo  ` → 去尾随 2 空格 → `Foo` → `<h2>Foo</h2>`。
- **87**：`Foo` 段落，下一行 `    ---`：`isThematicBreak` 因缩进 4≥4 返回 false → 不打断 → 续行 `text=Foo\n---` → 末尾无尾随空白 → `<p>Foo\n---</p>`。
- **93**（不修）：保持现状，**不得回归**。

## ArkTS 硬约束
- 无正则（`new RegExp`/`.replace(/../)`）；全 char-loop / `substring` / `trim`(已有)。
- 无匿名对象字面量（本批返回都是 number/boolean/string）。
- 无 Record / 无 `obj[key]` 对象属性下标；`s[i]` 字符串取字符合法。
- import 在顶部（本批不新增 import）。

## 运行 + 自验（容器内，node_modules 已预置）
```bash
cd /data/docs/TuiEditorHarmonyOS/tools/commonmark-spec
./node_modules/.bin/tsx run-spec.ts 2>&1 | tail -40
```
- 报告：总 exact（X/652、%）、**Setext headings 节前后（20→?）**、**Thematic breaks 节前后（15→?，可能因缩进守卫上升）**、error、hang。
- 必查样例 PASS/FAIL：**82, 84, 86, 87, 88, 89**（应全 PASS）；93 仍 FAIL（预存，确认未回归）。
- error 必须 0，hang 必须 0；关注 Paragraphs / Thematic breaks / Indented code blocks / Block quotes / Lists 不回归。

## 报告格式
1. 改了哪些函数。
2. harness 总 exact 前后 + Setext headings 节前后 + Thematic breaks 节前后。
3. 上面 6 个必查样例逐一 PASS/FAIL。
4. 你能看到的任何节回归。
5. ArkTS 自检（无正则/匿名字面量/Record/对象下标）。

> Hermes 会独立修权限 + ArkTS grep + 亲跑 harness + 全节回归脚本对比 git HEAD baseline。CC 自报数字仅供参考。
